version: '3.8'

services:
    test-runner:
        build: .
        volumes:
            - .:/usr/src/app
        working_dir: /usr/src/app
        command: npx mocha 'tst/integ/**/*.test.js'
        depends_on:
            - mock-registry

    mock-registry:
        image: registry:2.8.3
